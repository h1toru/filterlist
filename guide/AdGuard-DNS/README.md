## *Access settings* configuration
to save its monthly request limit

> [!IMPORTANT]
> This can only be configured manually
> (via Copy-Paste), you can't add this
> to or as a (remote) custom blocklist.
> 

# *Allowed clients* section

# *Disallowed clients* section
::/0

# *Disallowed domains* section
